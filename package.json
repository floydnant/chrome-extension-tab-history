{
  "name": "tab-history-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.js",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
